services:
  app:
    image: rafisharkar/smartsolutions-frontend:latest
    container_name: smartsolutions-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped

volumes:
  app_data:

networks:
  default:
    name: smartsolutions_network
